const SkillSetMisc = ({ icon }) => {
  return (
    <div className="text-[var(--primary-dark)] transition-all ease-in-out  hover:text-[var(--primary-light)] hover:scale-105">
      {icon}
    </div>
  );
};

export default SkillSetMisc;
